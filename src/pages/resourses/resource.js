import classes from './resource.module.scss';

const Resource = props => {
  return (
    <main className={classes.main}>
      <h1>Requesting file and server resources</h1>
    </main>
  )
}

export default Resource;